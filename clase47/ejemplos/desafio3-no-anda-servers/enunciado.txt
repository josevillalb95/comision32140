Modificar el servidor del desafío anterior, conservando la misma funcionalidad, para que utilice el módulo http servest generando la vista con React render.

El servidor deberá tener extensión tsx para el correcto funcionamiento de la sintaxis de vista de React en Typescript.

Configurar denon para que, ante un cambio de código, el servidor de reinicie automáticamente.
