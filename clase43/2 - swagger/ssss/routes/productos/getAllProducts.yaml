paths:
  /productos:
    get:
      summary: get all products
      tags:
        - Productos
      responses:
        "200":
          description: 'todos los productos'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Product'